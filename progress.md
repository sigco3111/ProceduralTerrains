Original prompt: In square mode the add-tile overlay must be square; circle mode must crop the terrain correctly without a circular add overlay; move tile information into the World tab.

- Diagnosed the circular assembly regression from commit 960991f.
- Planned fixes: restore square ghost geometry, disable tile-add hover interaction in circle mode, discard terrain fragments outside the circular mask, and merge Tiles controls into World.
- Implemented the geometry/shader/UI fixes. Verification pending.
- Fixed a live runtime error caused by the moved Tiles content missing its ControlSection import.
- Live verification: World contains Tiles, circle mode crops the square mesh cleanly and has no add preview, square mode shows a square add-tile preview.
- Removed an ANGLE shader warning by making assemblyFalloff assign both branches before mixing.
- Final production build passes and a clean browser startup reports no console warnings or errors.
- Follow-up requested: complete missing circle cells when switching from Square, support circular ring expansion, and make Mountains add edge noise instead of behaving like Island.
- Circle radius is now explicit and persisted in saves/undo. Each radius includes every square backing chunk intersected by the rendered disk, eliminating diagonal wedge gaps.
- Verified partial Square layout (2 tiles) becomes a complete radius-1 Circle (9 backing tiles), then expands through an all-around ring to radius 2 (25 backing tiles).
- Mountains now preserves the base terrain and adds deterministic ridged noise toward the outer boundary; CPU sampling matches the shader.
- Production build passes and the tested browser flow reports no console warnings or errors.
- Follow-up: remove the circular plinth's black top cap beneath water and suppress circular-mode chunk skirts that appear as dark tile seams.
- Implemented an open-top circular plinth (outer wall + bottom only), so lakes show the water material instead of a black cap.
- Disabled square chunk skirts in Circle mode; the circular plinth owns the perimeter wall and internal tile boundaries remain continuous.
- Verified top-down and close angled views after circle expansion: water renders normally, internal black seams are gone, and browser console is clean.
- Follow-up: eliminate circle seams at every LOD, align the outer wall with the real terrain edge, and redefine falloff width so 0 disables edge modification.
- Restored circle chunk skirts purely as LOD crack fillers (full depth, terrain-coloured, no darkening, no wall shading).
- Replaced the fixed-height circular cylinder with a dedicated radial wall rendered by the shared terrain height shader (aWall attribute): its top ring follows the exact island/mountain silhouette, its base drops to the plinth base. The circular plinth is now just the bottom cap.
- Separated circular clipping (hard diskMask) from island attenuation (inward diskIsland profile), removing the half-height boundary caused by centring the old smooth mask across the disk edge.
- Redefined Edge Falloff Width: 0 = no island attenuation and no mountain edge noise (terrain reaches the boundary unchanged); island values fade entirely inside the boundary; mountain values scale both band width and noise amplitude (0.05 is a subtle rim). UI min lowered 0.05 -> 0 in both control tables. CPU sampler formulas mirror the shader.
- Verified via the live engine: clean top-down disk with no chunk lines, radial wall tracking the mountain silhouette with no V-gaps, island fade into water with lakes (no black cap), correct falloff heights at 0/0.05/0.2/1 for island and mountains, square<->circle toggle and radius 0/1/2 expansion, no console warnings, production build passes.
- Follow-up bugs reported on the expanded circle: dark tile-edge seams between cells, and the cloud slab only covering the centre cell.
- Fixed the seams: circle disk chunks now suppress the skirt drop on interior cell boundaries (tileInteriorSeam), matching square mode, so the vertical crack-filler flap no longer reads as a dark line between tiles; intra-cell LOD skirts are kept.
- Fixed the clouds: CloudSlabLayer.applyParams now takes a tile-assembly layout { extent, center } so the slab radius/box/occupancy/far-march and the in-range cull all cover the whole union (feature size still per-cell); Engine passes the union extent + centre. Verified clouds blanket the full radius-2 disk and the expanded terrain is seam-free at a low rim angle. Production build passes.
- Third report: (a) dark tile edges still appear between tiles; (b) the circular wall outline follows the PREDICTED (analytic) terrain, not the VISIBLE mesh — thin dark whiskers poke above the silhouette, worst at the far rim.
- Diagnosed (b): the radial wall samples full-detail heightAt radially and captures sub-mesh edge-noise spikes that the coarse far-LOD terrain mesh smooths away, so the wall pokes above the terrain. Confirmed visually (thin dark spikes above the silhouette at a grazing rim view).
- Fixed (b): the wall TOP is now low-passed — averaged over 5 heightAt samples spanning ±2 coarsest-LOD quads (uChunkSize/8) of arc — so it tracks the visible terrain mesh instead of the analytic field. Build passes. NOT yet visually verified (test GPU context exhausted from automated render churn).
- (a) seams: added interiorSeam skirt suppression matches square mode's proven approach; in multi-angle testing (top-down, low water skim, oblique) the water/terrain looked continuous with no dark lines. Could NOT reproduce the reported dark tile lines — they may be stale (pre-fix) screenshots or LOD-transition height-step lines (not cell boundaries). Needs a precise repro (preset/seed/camera) on a working GPU.
- Follow-up: make first Infinite World entry reuse Tile's already-compiled terrain color shader instead of showing a minimal-color fallback and compiling a second INFINITE_MODE program.
- Replaced terrain's compile-time mode branches with the shared uInfiniteMode uniform; Tile and Infinite factories now emit identical full shader source/defines while keeping separate material objects.
- Removed the Infinite terrain fallback/upgrade and duplicate terrain warmups from mode entry, octave changes, and noise-stack rebuilds. Added material source-identity regression tests. Verification pending.
- Verification: the full Vitest suite passes (139 tests) and the production build succeeds.
- Live browser verification: first and repeated Infinite entries immediately showed the active full terrain palette with 175 / 441 chunks visible; Tile ? Infinite ? Tile ? Infinite transitions settled within the first observed second, with no `Loading full terrain colors` state.
- The tested Infinite frame rendered cleanly and the browser console contained no warnings or errors.
- Follow-up requested: keep manual-editor control panels above the Shape Library, make the Shape Library vertically resizable, and remove the backgrounds from its image thumbnails.
- Implemented a persisted Shape Library height with pointer and keyboard resizing. Manual-editor side drawers, the camera bar, and the minimap now use the live tray height; the tray also clamps itself on smaller windows so controls stay reachable.
- Thumbnail inspection confirmed all seven WebPs already contain alpha cutouts. Removed the CSS thumbnail fill, border, inset overlay, and framed shadows so the cutout artwork now displays without rectangular backgrounds.
- Verification: all 223 Vitest tests pass, the production build succeeds, and git diff whitespace checks are clean. After refreshing the live Chrome editor, Visuals aligned to the Shape Library with a measured 0 px gap/overlap at 214 px and 320 px tray heights; pointer and keyboard resizing worked, lower controls stayed scrollable, thumbnails rendered as transparent cutouts, and the tray was restored to 214 px after testing.
- Follow-up requested: replace the AI-looking raster Shape Library thumbnails with code-authored pixel art.
- Replaced every manual shape thumbnail with deterministic inline SVG pixel maps and restored per-category pixel palettes while keeping the background transparent. The raster assets are no longer referenced and can be removed.
- Follow-up requested: add direct painting of all shipped terrain textures to Manual Terrain with seamless material transitions.
- Added Texture Paint mode (T) with Paint, Blend, and Erase tools; Grass, Rock, Sand, Snow, Mud, Volcanic, and Alien swatches; adjustable size, strength, and edge blend; colored brush feedback; and sculpt/transform mutual exclusion.
- Added a seven-channel, two-dominant-layer weight field with soft coverage, continuous stroke spacing, save/load/resampling, undo-history blob deduplication, clear support, and triplanar shader blending. Manual projects now load a built-in atlas made from the shipped base textures.
- Packed normal XY, roughness, and AO into one surface-properties atlas to keep the complete terrain shader at the 16-fragment-texture GPU limit, and corrected atlas row orientation so each swatch maps to its named material.
- Verification: all 228 Vitest tests pass; the production build succeeds; browser testing created a Manual Terrain, loaded all seven thumbnails, painted Grass and Rock, blended their intersection, preserved a soft seam, enabled Clear texture layer, and reported no runtime warnings or errors.
- Follow-up requested: implement the Startup Lag Diagnosis fixes for the 26-second water boot regression.
- Removed water compilation from `_warmupInitialShaders`; post-first-paint work now owns water initialization and boot schedules that path even when the landing showcase is bypassed.
- Shader readiness now returns structured success/timeout data, compile logs separate synchronous translation from async link waiting, and water never swaps or reports precompiled after a timeout.
- Studio legacy/realistic water now uses the baked terrain-height texture with a minimal ripple-noise helper; procedural terrain height code remains only in Infinite water. Legacy water is single-pass.
- Initial water preparation now compiles only the effective requested material, avoiding a legacy-then-realistic double compile. Added startup/shader regression coverage; all 233 Vitest tests and the production build pass.
- Live SwiftShader verification preserved the intended order: first textured frame completed with `water ready=false`, then deferred Studio water compiled in 1 ms and initialized in 2 ms. Landing, Tile legacy/realistic/off, and Infinite mode rendered without shader/runtime errors; the only HTTP errors were the expected auth/analytics 500s because the standalone preview API was not running. Reviewed 1600×900 and 1280×720 screenshots with no water artifacts or viewport overflow.
- Follow-up: cold boot compiled the full terrain for the direct canvas, then the first Studio frame compiled it again for the post-processing scene target (43.4s + 23.6s in the reported trace). Boot also released before deferred water activation.
- Updated boot warmup to target the exact first-frame render target, overlap the baked-water compile with terrain linking, await water activation, and release the loading overlay only after rendering a frame containing both terrain and water.
- Verification: all 235 Vitest tests pass and the production build succeeds. A live RTX 5060 Ti run completed terrain warmup in 68 ms, water initialization in 33 ms, and its first textured frame in 9 ms (`terrain ready=true, water ready=true`, 228 ms total); the rendered editor showed ocean/lake water and produced no new warnings or errors.
- Follow-up: cold starts on slower GPUs could still soft-lock because boot waited for the full terrain shader, then redundantly polled its program for up to five more minutes; the App watchdog refused to release while `_bootPending` or `_compiling` remained set.
- Boot now compiles and renders the lightweight terrain/visible-scene variant behind the overlay. Height baking, full terrain colour and water are staggered afterward, retain long background compile windows for slower GPUs, and each failed quality upgrade gets at most one bounded retry.
- Removed the redundant five-minute terrain and planet readiness polls, paced shader readiness checks to animation frames, made boot release idempotent, installed the 15-second watchdog before engine construction, and abort shader waits on WebGL context loss.
- The lightweight frame is only a hidden safety net: normal boot release now waits for full terrain colour, water activation, completion of the progressive board build, and one final rendered frame. The 15-second watchdog remains the sole early-release path for a genuinely stuck driver.
- Verification: all 239 Vitest tests pass and the production build succeeds. In a live medium-tier run, the `Starting terrain editor` state and disabled actions were still present after the hidden fallback frame at 478 ms. The terrain compile completed in 3.29 s, then boot released only after the final quality frame at 5.49 s; the loading state was gone and actions were enabled afterward.
- Follow-up: branch `0c21c2a` regressed cold/refresh boot, showed stale-height water and caustics, and restored dark multi-tile skirt lines; the requested target is v1-style rendering with a responsive progressive boot.
- History audit found the loader waiting on Full terrain linking, final height/biome baking, water, and complete LOD/chunk work. Preview bakes also published approximate water masks before the visible terrain changed, while every occupied cell edge rendered an overlapping vertical skirt.
- Boot now releases after a successfully rendered coherent Base safety frame. Full terrain, final bakes, water, and remaining chunks continue as bounded background quality work; a compatible Base material also completes procedural project creation instead of turning a Full-quality timeout into a fatal loading task.
- Studio water remains hidden until a generation- and layout-matched final height/biome bake publishes atomically. Preview bake renders were removed, sea-level changes invalidate the field immediately, and stale async water links can no longer reactivate an old generation.
- Interior square and circle cell skirts are suppressed at occupied boundaries. Terrain caustics use the actually rendered vertex height and exclude skirts/walls, preventing caustics above visible dry terrain.
- Shader and water retries are capped, warm materials survive through a live render, concurrent terrain-variant compiles are rejected, and the climate cache uses explicitly initialized scalar paths. Chrome no longer reports terrain-program validation, context-loss, or uninitialized-climate warnings.
- Verification: all 355 Vitest tests pass, the production build succeeds, and `git diff --check` is clean. Fresh Chrome reached Ready about 6 seconds after procedural creation; 3-cell square and 9-cell circle layouts at elevated sea level showed continuous terrain, one coherent waterline, submerged-only caustics, and no internal dark lines. The isolated SwiftShader cold-run rendered the landing terrain without a stuck loader; only the expected standalone auth/analytics API 500s remained.
- Removed the experimental five-sample circular-wall filter after a clean A/B Chrome run proved that its repeated full terrain evaluations caused program validation/context failures. The radial wall now uses the original single height lookup; the radius-1 close preview showed a clean rim without dark internal seams.
- Final startup-order audit found two additional blockers: a live template preview rendered a throwaway project immediately before creation, and a compatible minimal terrain was forced through an atomic Full-material/cache compile. Template selection no longer renders behind the landing page; new projects install seed, preset, and the default stack before their single terrain application, and a matching boot material completes the transition without acquiring a source token.
- Blocking readiness now follows the active terrain-source transaction instead of the global compile counter. Optional warmups require a 3.5 s input-quiet window, remain disabled behind the landing showcase, and retrying a Base upgrade always re-schedules the requested Detail variant.
- Reordered post-paint work so the final generation-matched height/biome cache and water initialize before optional surface-fragment translation. On the RTX 5060 Ti production run the landing became ready in 1.96 s, project overlay closed in 0.32 s, and correct water activated in 5.01 s; water mesh Y, terrain/water sea uniforms, and seaLevel all matched at 100, with bake generation 8/8.
- Production source captures showed continuous water/terrain with no internal dark chunk lines; only the intended board plinth remained dark. Caustics are clipped using rendered vertex height, excluded within the one-unit surface band, and rejected on skirts/perimeter walls, so they cannot render above water.
- Final verification: all 376 Vitest tests pass, the production build succeeds, git diff --check is clean, and a clean restarted production preview opened the procedural editor in 0.26 s after selection with no runtime errors or diagnostic globals.
- Completion audit reproduced the last refresh defect directly: reloading 8 s after procedural creation took 14.78 s to reach DOMContentLoaded even though the next engine initialized in 74 ms. Control reload before optional quality work took 35 ms, proving the delay was an in-flight ANGLE terrain-fragment translation that the browser cannot cancel, not React/project loading.
- The first full Base terrain translation now waits for a 15 s input-idle window. Height/biome baking and generation-matched water still run first, and Base then Detail still upgrade in the background. Added regression coverage for water-before-terrain scheduling and the extended reload-safe idle gate.
- Final RTX 5060 Ti production rerun: cold landing 1.77 s, create overlay 0.50 s, water ready before the quality job, reload DOM 34 ms / landing ready 190 ms, saved-project reopen 1.04 s. Fresh and reopened camera drags changed the rendered frames; both inspected captures had continuous terrain/water and no internal dark lines. No page, shader, validation, context-loss, or unexpected HTTP errors occurred.
- Long-idle verification completed the Base terrain upgrade at 37.02 s in the deliberately off-screen/throttled browser (20.19 s driver compile after the idle gate); reload after completion remained 33 ms / 157 ms ready. This proves the v1-quality upgrade remains enabled without owning boot or refresh.
- Final verification after the refresh fix: all 377 Vitest tests pass, the production build succeeds, git diff --check is clean, diagnostic browser files were removed, and the preview process was stopped.